<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.auctionhouseapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_TO_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/auction_app_face"
        android:label="@string/app_name"
        android:roundIcon="@drawable/auction_app_face"
        android:supportsRtl="true"
        android:theme="@style/Theme.AuctionHouseApp">

        <activity
            android:name=".LogInActivity"
            android:exported="false">
            <intent-filter>
                <action android:name="package.OtherActivity" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AuctionHouseApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>